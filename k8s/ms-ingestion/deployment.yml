apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-ingestion
  labels:
    app: ms-ingestion
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-ingestion
  template:
    metadata:
      labels:
        app: ms-ingestion      
      annotations:              
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"

    spec:
      containers:
      - name: ms-ingestion
        image: jpedroduarte123/agtc-srv-ingestion:latest
        ports:
        - containerPort: 8080

        envFrom:
        - secretRef:
            name: secrets-ms-ingestion

        livenessProbe:
          httpGet:  
            path: /health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 20

        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10

        resources: 
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "300m"
            memory: "512Mi"